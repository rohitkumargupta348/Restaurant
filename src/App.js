
//rafce-react arrow function with component
import React from 'react'
import Resturant from './component/basics/resturant'

const App = () => {
  //we can use section, article, div, React.Fragment(or <>) to return jsx
  return (
    //jsx
    <div>
     <Resturant />
    </div>
  )
}

export default App

//node module :- tells us the dependencies we have installed
//two components cannot have same name

